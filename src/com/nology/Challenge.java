package com.nology;

public class Challenge {

    public Challenge() {
    }

    // write the code the programmatically return the largest number from the 2 parameters (num1, num2) passed to the method
    public int findLargestNumber(int num1, int num2) {
        return Math.max(num1, num2);
    }
}
